{
  "name": "figmify",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "build": "cd frontend && npm install && npm run build",
    "dev": "cd backend && npm install && npm run dev"
  },
  "dependencies": {},
  "devDependencies": {}
}